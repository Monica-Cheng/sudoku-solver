# Pinned to the versions verified during Phase 1 cleanup (Python 3.11).
tabulate==0.10.0
imutils==0.5.4
matplotlib==3.11.1
numpy==1.26.4
tensorflow==2.20.0
keras==3.15.1                      # model_fonts_mnist.keras is a Keras 3 artifact
opencv-python-headless==4.11.0.86  # was opencv-python; this repo makes no cv2 GUI calls
