# docs/

Screenshots referenced by the top-level `README.md`. Add:

| file | what to capture |
|---|---|
| `race-mode.png` | `/solve?...&mode=race` — the 2×2 grid with all four algorithms mid-solve, live counters visible |
| `benchmarks.png` | `/benchmarks` — the solve-rate matrix and the node-count log-scale bars |

PNG, roughly 1600px wide, taken in the dark theme.
